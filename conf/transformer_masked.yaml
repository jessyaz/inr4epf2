registry: masked_transformer
model_uid: null
run_dir: null
exp_suffix: null

seed: 0
device: auto

dataset:
  name: DE
  processed_dir: ./datasets/processed
  batch_size: 64
  num_workers: 4

window:
  lookback: 168      # un jeton par heure
  horizon: 24
  stride_train: 6    # aucune contrainte d'alignement journalier
  stride_eval: 24

masking:
  rate: 0.0
  mechanism: mcar
  block_mean: 12

train:
  num_epochs: 300
  patience: 30
  plot_every: 0
  save_checkpoint: true
  warmup_steps: 500
  grad_clip: 1.0

model:
  # seule ligne qui distingue les deux variantes
  masked_attention: true
  imputation: linear          # utilisee par la variante imputee seulement

  embedding_dim: 64
  num_heads: 4
  dim_feedforward: 128
  num_layers: 3
  normalize_first: true
  dropout: 0.1
  activation: gelu

  exog_dim: 2
  use_lookback: true

  optim:
    lr: 3e-4
    weight_decay: 1e-4

pod:
  model_uid: null
  rates: [0.1, 0.2, 0.3, 0.5, 0.7, 0.9, 0.95, 1.0]
  mechanisms: [mcar, block]
  mask_seeds: [0, 1, 2, 3, 4]
  recalib_on_masked: false
  run_ablation: true
  out_dir: ./results/pod

mlflow:
  enabled: true
  force_offline: false
  experiment_name: null
  run_name: null